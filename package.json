{
  "name": "json-to-sql-script",
  "version": "0.1.2",
  "description": "Convert your JSON-file to SQL-script for inserting to database.",
  "main": "json-to-sql-script",
  "bin": {
    "json-to-sql-script": "./bin/json-to-sql-script"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "json",
    "sql"
  ],
  "author": "Vit Tertiumnon",
  "license": "ISC",
  "devDependencies": {
    "cz-conventional-changelog": "^3.2.0",
    "eslint": "^5.16.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.17.2"
  },
  "dependencies": {
    "arg": "^4.1.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
